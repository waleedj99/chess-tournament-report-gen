export const redirectToGame = (gameId) => {
  // This is a placeholder function. In a real application, you would implement
  // the actual redirection logic here, possibly using React Router or
  // window.location depending on your setup.
  console.log(`Redirecting to game with ID: ${gameId}`);
  // Example redirection (uncomment and modify as needed):
  // window.location.href = `/game/${gameId}`;
};